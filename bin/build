#!/usr/bin/bash

debug="-d:NYTProf"
cd ~/scramble && perl $debug  bin/parse --dictionary-dir dict --rep data/gabrielx/reports --images data/gabrielx/images --tem data/gabrielx/templates --out html -v "$@"
